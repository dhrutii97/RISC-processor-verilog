# RISC-processor-verilog
This project implements a basic unpipelined RISC processor structured around the classical 5-stage datapath: Instruction Fetch (IF), Instruction Decode (ID), Execute (EX), Memory Access (MEM), and Write Back (WB).
